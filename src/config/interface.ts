interface configOption {
    axios: {
        baseURL: string
        timeout: number
    }
    layout: {
        sider: {
            width: number
            collapsedWidth: number
        }
        theme: string
    }
    locale: string
}